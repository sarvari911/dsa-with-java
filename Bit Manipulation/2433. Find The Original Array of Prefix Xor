class Solution {
    public int[] findArray(int[] pref) {
        int[] orgArr = new int[pref.length];
        orgArr[0] = pref[0];
        for(int i = 1; i < pref.length; i++){
            orgArr[i] = pref[i]^pref[i-1];
        }
        return orgArr;
    }
}
